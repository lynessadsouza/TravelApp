import { StyleSheet } from 'react-native';

export  const styles = StyleSheet.create({
   container: {
     margin: 32,
     backgroundColor: 'white',
   },

   mainContainer: {
    flex: 1,
    backgroundColor: 'white',
   },


   subtitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'black',
    marginTop: 40,
    marginBottom: 36
   },

 });
